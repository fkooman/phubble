<!DOCTYPE html>

<html lang="en">
{% include 'head.twig' %}
<body>
    <div id="container">
        {% include 'header.twig' %}

        {% if indieInfo %}
            <p>
You are already logged in as <code>{{ indieInfo.getUserId|e }}</code>.
            </p>
            <form method="post" action="{{ root|e }}_indieauth/logout">
                <input type="submit" value="Logout">
            </form>
        {% else %}

            <h2>Sign In</h2>
            <p>
    Sign in using your domain!
            </p>

            <form class="authenticate" method="post" action="{{ root|e }}_indieauth/auth">
                <label for="me">https://</label><input id="me" required type="text" name="me" placeholder="example.org">
                <input type="hidden" name="redirect_to" value="{{ rootUrl|e }}_my_spaces/">
                <input class="info" type="submit" value="Sign In">
            </form>
        {% endif %}

        {% include 'footer.twig' %}
    </div> <!-- /container -->
</body>
</html>
