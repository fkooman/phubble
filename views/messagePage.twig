<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Message Board</title>

  <link rel="stylesheet" type="text/css" href="{{ rootFolder|e }}css/normalize.css">
  <link rel="stylesheet" type="text/css" href="{{ rootFolder|e }}css/style.css">

  <link rel="micropub" href="{{ rootUrl|e }}{{ space|e}}/_micropub">
</head>

<body>
    <div id="container">
        <a class="header" href="{{ root|e }}"><h1>Phubble - {{ space|e }}</h1></a>

        {% if not user_id %}
            <form class="try" method="post" action="{{ rootUrl|e }}_indieauth/auth">
                <span class="label">https://</span><input required type="text" name="me" placeholder="example.org">
                <input type="hidden" name="redirect_to" value="{{ rootUrl|e}}{{ space|e}}/{{ message.id|e }}">
                <input type="submit" value="Sign In">
            </form>
        {% endif %}

        {% if message.author_id == user_id %}
        <form method="post" action="{{ message.id|e }}">
            <input type="hidden" name="_METHOD" value="DELETE">
            <input type="submit" class="warn" value="Delete">
        </form>
        {% endif %}

        <table class="message h-entry">
            <tr>
                <th>
                    <a class="p-author" href="{{ message.author_id|e }}">{{ message.author_id|e }}</a>
                    <span class="date">{{ message.post_time|e|date }}</span>
                </th>
            </tr>
            <tr>
                <td class="e-content">{{ message.message_body|raw }}</td>
            </tr>
        </table>

        <div class="footer">
            <span class="pull-left">Phubble</span>
            <span class="pull-right">
            {% if user_id %}
                {{ user_id|e }} | <form method="post" action="{{ rootUrl|e}}_indieauth/logout"><input type="submit" value="Logout"></form>
            {% endif %}
            </span>
        </div> <!-- /footer -->

    </div> <!-- /container -->
</body>
</html>
